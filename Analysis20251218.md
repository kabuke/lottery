# 專案解析報告 (Analysis20251218.md)

本文件基於 2025-12-18 的專案狀態，對比原始需求規格 (`specs/001-lottery-draw/spec.md`)、任務列表 (`specs/001-lottery-draw/task.md`) 與實際程式碼實作進行解析。

## 1. 專案概觀 (Project Overview)

本專案為一個「多租戶 (Multi-tenant) 網頁版抽獎應用程式」，採用 **Go (Gin)** 作為後端，**HTMX** 處理前端動態互動，並使用 **In-Memory** 結構存儲資料。目前的實作版本已具備完整的功能核心，包括獎項與參與者管理、抽獎邏輯、CSV 匯入匯出以及租戶隔離機制。

## 2. 結構與架構 (Structure & Architecture)

專案結構完全符合 `specs/001-lottery-draw/plan.md` 的規劃：

- **Entry Point:** `cmd/main.go` 負責初始化服務、載入嵌入式資源 (Templates/Assets) 與路由設定。
- **Handlers:** `internal/handlers/http_handlers.go` 處理 HTTP 請求、CSV 解析與 HTMX 回應。
- **Service:** `internal/services/lottery_service.go` 封裝核心商業邏輯 (抽獎、名單過濾)。
- **Models:** `internal/models/lottery.go` 定義資料結構。

**特點:**
- **In-Memory Storage:** 使用 `map[string]*LotterySession` 儲存所有資料，並透過 `sync.RWMutex` 保證併發安全。
- **Tenant Isolation:** 透過 Cookie (`lottery_tenant_name`) 與 Client IP 組合生成唯一 `TenantID`，確保不同使用者的資料完全隔離。
- **Session Cleanup:** 實作了背景 `CleanUpInactiveSessions` 機制，自動回收閒置資源。

## 3. 功能實作狀態 (Feature Implementation Status)

對照 `specs/001-lottery-draw/spec.md` 與 `task.md`：

### 3.1. 獎項與參與者管理 (已完成)
- **手動新增:** 已實作 API (`POST /prizes`, `POST /participants`) 與前端表單。
- **CSV 匯入:** 已實作 CSV 解析邏輯，支援批量匯入。
    - 獎項 CSV 格式正確處理了 4 個欄位 (含 `DrawFromAll` 標記)。
    - 參與者 CSV 格式正確處理了 ID 與 Name。
- **列表顯示:** 前端樣板 (`prize_list_container.html`, `participant_list_container.html`) 結合 HTMX 實現局部更新。

### 3.2. 抽獎邏輯 (已完成)
- **核心演算法:** `Draw` 方法使用 `math/rand` (Go 1.20+ 自動 Seed) 進行隨機抽取。
- **資格判定 (`GetEligibleParticipants`):**
    - **一般獎項 (`DrawFromAll=false`):** 嚴格限制 `len(wins) == 0`，確保每人只能在一般獎池中中獎一次。
    - **特殊獎項 (`DrawFromAll=true`):** 檢查 `!wins[prizeName]`，允許已中過其他獎項的人再次參與，但同一特殊獎項不能重複中獎。此邏輯符合「全體參與 (但單一獎項不重複中)」的合理詮釋。
- **狀態更新:** 中獎後立即扣除獎項數量並記錄中獎者。

### 3.3. 結果與報表 (已完成)
- **即時顯示:** 抽獎後透過 HTMX 動態更新中獎名單與動畫。
- **CSV 匯出:** `ExportResultsCSV` 實作了結果下載功能，且加入 BOM (`\xef\xbb\xbf`) 確保 Excel 開啟時中文不亂碼。

### 3.4. 測試與部署 (進行中)
- **單元測試:** 專案中已有 `internal/services/lottery_service_test.go`。
- **整合測試:** `task.md` 中標記為未完成 (`[ ] 任務 6.2`)。
- **部署腳本:** 根目錄已有 `build.sh`，符合 `任務 6.3`。

## 4. 程式碼分析與建議 (Code Analysis & Suggestions)

### 優點
1.  **結構清晰:** 職責分離明確，Handler 不包含複雜邏輯，Service 專注於狀態管理。
2.  **安全性:** 針對 Tenant 機制做了 Cookie 驗證與 IP 綁定，避免資料混淆。
3.  **防呆機制:** 
    - `Draw` 前會再次檢查 `GetEligibleParticipants`。
    - CSV 讀取有基本的錯誤處理與格式檢查。

### 觀察到的細節
1.  **抽獎資格邏輯:**
    如果一位參與者先中了「特殊獎項 (DrawFromAll=true)」，他的 `wins` 紀錄就不為空。接著若進行「一般獎項」抽獎，該參與者會因為 `len(wins) != 0` 而被排除。
    *這符合「每位參與者只能中獎一次 (一般情況)」的原則，意味著中了特殊獎項也會消耗掉一般中獎的機會。若需求是「特殊獎項不佔用一般中獎名額」，則邏輯需調整。但依照目前的 Spec 描述，此實作是合理的。*
2.  **前端資源:**
    `main.go` 使用 `embed` 打包了 `templates` 與 `assets`，確保編譯出的單一執行檔即包含所有資源，這非常便於部署。

## 5. 結論

目前的程式碼實作完整度很高，核心功能皆已就緒。
建議接下來的工作重點可以放在：
1.  **驗證 UI/UX:** 實際執行 `build.sh` 並試跑，確認動畫與 HTMX 互動的流暢度。
2.  **完善測試:** 補足整合測試，模擬多租戶同時操作的情境。
3.  **部署準備:** 雖然已有 `build.sh`，可進一步準備 Dockerfile 或 Systemd service file 以利正式部署。
